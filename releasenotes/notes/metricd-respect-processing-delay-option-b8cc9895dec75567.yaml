---
features:
  - |
    Metricd exposes a new option called `respect_processing_delay` (false by
    default) that allows to control whether eager processing of new measures is
    enabled when available.
